# The agent + eval harness needs only these three:
#     pip install anthropic pydantic pytest
# Everything else below is for the SaaS milestones that are not built yet.

# Core framework
fastapi>=0.115.0
uvicorn[standard]>=0.32.0

# Anthropic SDK
anthropic>=0.40.0

# LLM routing / multi-provider
litellm>=1.50.0

# Database
sqlalchemy[asyncio]>=2.0.0
asyncpg>=0.30.0

# Cache + rate limiting
redis[asyncio]>=5.0.0

# Semantic cache (embeddings)
openai>=1.50.0
numpy>=1.26.0

# Observability
# langfuse>=3.0.0      # optional

# Billing
stripe>=11.0.0

# Auth
python-jose[cryptography]>=3.3.0

# Pydantic models
pydantic>=2.9.0
pydantic-settings>=2.6.0

# Dev / testing
pytest>=8.0.0
pytest-asyncio>=0.24.0
httpx>=0.28.0
