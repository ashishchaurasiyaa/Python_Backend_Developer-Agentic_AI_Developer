[pytest]
testpaths = tests
# pytest-odoo is installed globally on this machine and imports odoo at
# collection time, which breaks unrelated suites. Blocking a plugin that isn't
# installed is a no-op, so this is safe everywhere.
addopts = -q -p no:odoo
