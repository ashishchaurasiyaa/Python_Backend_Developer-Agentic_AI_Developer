# Lab 02 — reverse proxy config. Yeh file docker-compose ke `nginx` service
# me /etc/nginx/conf.d/default.conf pe mount hoti hai.
#
# TODO 1, 2, 3 bharo, phir: docker compose up -d nginx backend

# ─────────────────────────────────────────────────────────────
# TODO 1: upstream block banao jo docker-compose service "backend"
#         (port 8000) ko point kare. Compose network me service
#         naam hi DNS hostname ban jaata hai.
#         Hint:
#         upstream backend_pool {
#             server backend:8000;
#         }
___TODO_1___
# ─────────────────────────────────────────────────────────────

server {
    listen 80;

    location / {
        # ─────────────────────────────────────────────────────
        # TODO 2: request ko upstream backend_pool ko forward karo.
        #         Hint: proxy_pass http://backend_pool;
        ___TODO_2___
        # ─────────────────────────────────────────────────────

        # ─────────────────────────────────────────────────────
        # TODO 3: client ka real IP (aur koi existing forwarded
        #         chain) backend tak header se pahunchao.
        #         Hint: proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        ___TODO_3___
        # ─────────────────────────────────────────────────────
    }
}
