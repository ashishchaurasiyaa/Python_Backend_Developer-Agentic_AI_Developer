fastapi>=0.111.0
uvicorn[standard]>=0.30.0
sqlalchemy[asyncio]>=2.0.0
asyncpg>=0.29.0
redis[asyncio]>=5.0.0
qrcode[pil]>=7.4.2
python-jose[cryptography]>=3.3.0
bcrypt>=4.1.0
python-dotenv>=1.0.0
pydantic-settings>=2.0.0
email-validator>=2.1.0

# Not used at this project's current scope, kept here as a documented
# upgrade path — see README "What's deferred": aiokafka + a Clickhouse
# client would be added when the click-analytics pipeline (spec section 9)
# gets built out.

# --- testing ---
pytest>=8.0.0
pytest-asyncio>=0.23.0
httpx>=0.27.0
